# Core dependencies for high-quality audio source separation
torch>=2.5.0
torchaudio>=2.5.0
numpy>=1.24.0
scipy>=1.10.0

# Audio processing and I/O
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0  # For broader format support

# Source separation models
demucs>=4.0.0  # Core separation models

# UI and progress display
rich>=13.3.0  # Rich terminal output
tqdm>=4.65.0  # Progress bars

# Configuration and utilities
python-dotenv>=1.0.0  # Environment variables
pyyaml>=6.0  # YAML configuration
click>=8.1.0  # CLI utilities

# Performance monitoring (optional but recommended)
psutil>=5.9.0  # System resource monitoring

# Parallel processing
joblib>=1.3.0  # Parallel computation

# Data handling
pandas>=2.0.0  # Data manipulation (for batch processing)

# API support (if using external services)
requests>=2.28.0
aiohttp>=3.8.0  # Async HTTP requests

diffq
# Development tools (optional - install with pip install -r requirements-dev.txt)
# pytest>=7.0.0
# black>=23.0.0
# pylint>=2.17.0
# jupyter>=1.0.0
# ipykernel>=6.0.0